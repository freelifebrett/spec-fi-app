// validate.js
export const validatePersonalInfo = (personalInfo) => {
    let errors = {};
    // Add validation logic for personalInfo
    return errors;
  };
  
  export const validatePaymentDetails = (paymentDetails) => {
    let errors = {};
    // Add validation logic for paymentDetails
    return errors;
  };
  
  export const validateReferences = (references) => {
    let errors = {};
    // Add validation logic for references
    return errors;
  };
  
  export const finalValidation = (allData) => {
    let errors = {};
    // Add final combined validation logic
    return errors;
  };
  
  export default {
    personalInfo: validatePersonalInfo,
    paymentDetails: validatePaymentDetails,
    references: validateReferences,
    finalValidation: finalValidation,
  };
  